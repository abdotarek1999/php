1- locally : git branch -d dev
	     git branch -d test
2-remote: git push origin :dev
	  git push origin :test
	  
	  
list all tags:	git tag

1-locally:	git tag -d v1.7

2-remote:	git push origin --delete v1.7	  
